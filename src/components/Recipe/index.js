import React from "react"
import PropTypes from "prop-types"

const Recipe = ({}) => (
  <div
    style={{
      width: 960,
      margin: "0 auto",
    }}
  ></div>
)

Recipe.propTypes = {}

Recipe.defaultProps = {}

export default Recipe
